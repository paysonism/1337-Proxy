const config = {
  challenge: false, // Toggles password protection on the site
  users: {
    username: "password",
  },
  routes: true, // set to false if you just want to host a bare server.
  local: true, // set to false to disable local assets.
}
export default config
